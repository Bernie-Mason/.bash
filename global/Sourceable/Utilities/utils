# Handle graceful handling of errors

function say() {
	echo "$@" >&2
}

function die() {
	say "$2"
	exit "$1"
}

function isdirectory() {
	if [ -d "$1" ]; then
		return 0
	else
		return 1
	fi
}